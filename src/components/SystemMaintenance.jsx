import { RefreshCw, ShieldAlert } from "lucide-react";

export default function SystemMaintenance({ reason }) {
  return (
    <main className="flex min-h-screen items-center justify-center bg-white px-5 py-10">
      
    </main>
  );
}